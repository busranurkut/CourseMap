# Screenshots

Place screenshots here to display them in the project README.

Suggested captures (run `npm run dev` and take full-page screenshots):

- `homepage.png` — the landing page (hero + "Why not just ask AI directly?")
- `new-evaluation.png` — the New evaluation form (mode selector + framework)
- `report-tabs.png` — a report showing the Teacher evaluation / AI interpretation tabs
- `recipes.png` — the Adaptation recipes browser
- `literature-basis.png` — the Literature basis page

On macOS you can capture a browser tab with `Cmd+Shift+4` (then space to capture a
window), or use your browser's built-in full-page screenshot tool.
